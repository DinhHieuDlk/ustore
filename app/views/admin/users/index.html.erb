<p id="notice"><%= notice %></p>

<h1>Listing Admin Users</h1>

<table>
  <thead>
    <tr>
      <th>email</th>
      <th >admin</th>
      <th>action</th>
    </tr>
  </thead>

  <tbody>
    <% @admin_users.each do |admin_user| %>
    <tr>
    <td><%=admin_user.email%></td>
    <td><%=admin_user.admin%></td>
    <td>
    <%= link_to 'Edit', edit_admin_user_path(admin_user) %>
    <%= link_to 'Destroy', admin_user_path(admin_user), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_admin_user_path %>
